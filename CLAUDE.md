# Claude — Context for this repo

**Read `AGENTS.md` first** — it is the canonical source for commands, env
vars, architecture, database schema, slash commands, and conventions/gotchas.

**Planning during development:** use the local file `PLAN.md` to plan out all
work and track progress. `PLAN.md` is gitignored (never commit it).